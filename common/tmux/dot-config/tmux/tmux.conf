
if-shell '[ command -v nvim >/dev/null ]' {
    setenv EDITOR "nvim"
}

source-file ./tmux.conf.settings
source-file ./tmux.conf.keybinds
source-file ./tmux.conf.style

# load plugins last
source-file ./tmux.conf.plugins



